package com.renho.scalalang

/**
 * @author renho
 */
object Main {
  def main(args: Array[String]): Unit = {
    val i, j = 0
    val x = for(i <- 0 until 1 * 10000) {
      println("i:" + i)
    }
  }
}